# 待办

## 旧任务卡片隐藏操作区

状态：已实现并通过本地回归，随多机器人版本一起发布。

- 记录于 2026-09-12。用户要求先记录，等多机器人方案讨论完成后再一起实施。
- 同一会话成功发出下一张任务卡片后，旧任务卡片保留正文，隐藏全部交互控件。
- 包括 Agent、工作目录选择器，刷新、各模式开关、重启 Agent、删除会话，以及 Ctrl-C、Esc、Enter、打开终端和自定义快捷键。
- 最新任务卡片保留操作区，避免用户点击旧卡片后才收到“已失效”提示。
- 按机器人及会话隔离处理，不影响其他会话的最新卡片。

## 多机器人实施验收（2026-09-12）

- 已实现：单服务、多机器人隔离；机器人名称/应用凭证/默认 Agent/目录设置；仅展示所选机器人的会话；服务级 status、stop、restart；去掉端口与 all 选择；开机自启位于安全设置。
- 已验证：全量 Go 竞态测试；真实浏览器 PTY 执行、WebSocket 路由、配置认证/保存、改名、上传和删除隔离；1440/1024/760/390 宽度；旧卡片下一张成功后移除控件、失败保留、跨重启恢复与延迟更新。
- 已实测：Aiden X Codex、Aiden X Claude Code 的启动、完成通知和精确会话恢复，回调使用 `/bots/hook-test` 前缀。
- 已构建：macOS arm64、Linux amd64、Windows amd64。
- 本地自动建应用契约测试已通过，覆盖应用身份权限、数据范围收窄、消息/卡片事件回读、发布状态及失败不报成功。
- 未验证：真实飞书应用创建、权限配置、发布及测试卡片。用户已明确要求本轮跳过需要授权的真实验证，继续后续回归与发布；本地契约测试通过不代表此项真实验收通过。
- 本次真实创建验证在登录页等待 10 分钟后超时，未创建应用；独立浏览器及其临时登录资料已清理。后续需要补验时，运行 `IRIS_FEISHU_LIVE_TEST=1 go test ./cmd -run '^TestFeishuCreationLive$' -v -count=1 -timeout=11m`。
- 额外环境限制：本机直接运行 Claude Code 未登录；原生 Aiden 当前默认使用 Codex，原有原生 Aiden 测试的 `--print` 参数不适用。未修改用户的 Agent 登录或默认配置。
- 用户澄清：希望本次联调授权优先直接发可点击链接，不是要求修改产品的授权界面或登录态保存策略。误改的授权入口与持久化浏览器资料逻辑已撤回。
- 发布回归：`v0.2.67` 在发布前验证被拦截，未发布包；三个旧测试隐式依赖机器安装 Codex/Aiden，现改为临时命令夹具，不改变实际 Agent 检测。使用 `v0.2.68` 重新发布，不覆盖旧标签。
